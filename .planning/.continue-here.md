---
context: default
phase: 2-swift-windows-interop
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-06-24T12:00:00+02:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GitHub repo scope — `gh` defaults to `limehq/munkel`; fork PRs/commits use `--repo rodgi040/munkel`
- [ ] CONSTRAINT: Dev port collision — port 5173 is used by Ultra-Wisper-Flow; Munkel dev uses auto-picked port from 5174+ via `dev.mjs`

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong gh repo | `gh pr create` without `--repo rodgi040/munkel` fails or targets upstream | blocking | Always pass `--repo rodgi040/munkel` for fork work |
| Assuming Quit stops dev stack | Tray Quit only exits Electron; `node scripts/dev.mjs` + Vite may keep port 5174+ | advisory | Check `netstat`/process list; Ctrl+C terminal or add cascade shutdown in dev.mjs |
| gsd-sdk query subcommands | `gsd-sdk query init.resume` / `commit` not available in installed SDK | advisory | Use manual git/gh + read State.md/Roadmap.md |

<current_state>
Branch: `platform/windows/swift-windows-interop` (off `platform/windows/v2-clean`).

Phase 2 (Swift ↔ Windows interop) is **implemented but uncommitted**. Windows verification green (116 tests, 18 new interop-vector tests). User ran manual app test; Electron Quit was clean (0 processes, named pipe gone). Dev server was later stopped.

Private project state lives in `State.md` / `Roadmap.md` (gitignored), not `.planning/phases/`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Interop vectors (`scripts/interop-vectors/`), generator, `swift-interop.test.ts`, `InteropVectorsTests.swift`, `sealWithNonce` — Done
- Task 2: Auto free dev port in `dev.mjs` — Done
- Task 3: Manual smoke test (tray, quit) — Done; Electron shutdown clean
</completed_work>

<remaining_work>

- Task 4: Commit Phase 2 + PR → `platform/windows/v2-clean`
- Task 5: `swift test` on macOS (InteropVectorsTests)
- Task 6: Merge PR; Phase 3 `notch-reply-polish` or document manual-test issues
</remaining_work>

<decisions_made>

- Shared golden vectors in `scripts/interop-vectors/vectors.json` for cross-platform crypto/payload/codec parity
- Fixed-nonce sealing for deterministic Swift ↔ Windows blob comparison
- Dev port: probe from 5174 upward (`VITE_DEV_PORT` override)
- imageFixtures SHA-256 omitted when Bun lacks OffscreenCanvas (graceful degrade)
</decisions_made>

<blockers>
None technical. macOS Swift test run pending human/machine with Xcode.
</blockers>

## Required Reading (in order)

1. `State.md` — private integration state, parity gaps, fork strategy
2. `Roadmap.md` — milestone checklist
3. `scripts/interop-vectors/README.md` — how to regenerate vectors
4. `AGENTS.md` — branch/PR rules (`platform/windows/v2-clean`, no self-merge unless asked)

## Infrastructure State

- Dev server: **stopped** (no `dev.mjs` process; port 5174 free at pause time)
- Ultra-Wisper-Flow: may still use port **5173**
- Relay (`apps/server` :8787): not required for UI smoke test; needed for live chat
- Control pipe: absent when app not running (`\\.\pipe\Munkel-rodgi-Control`)

## Manual test notes (2026-06-24)

- App started on `http://127.0.0.1:5174`
- Tray Quit: Electron exited cleanly; control pipe removed
- Dev `bun run dev` wrapper did not auto-exit on Quit (expected current behavior)
- User feedback on UX bugs not yet captured — ask on resume if any

<context>
Phase 1 merged via PR #11. Phase 2 code is ready for commit/PR. Next session should commit feature work (exclude graphify artifacts unless intentional), open PR with `--repo rodgi040/munkel`, optionally run macOS swift test, then merge or iterate on manual-test findings.
</context>

<next_action>
Start with: `git status` on `platform/windows/swift-windows-interop`, commit Phase 2 files (exclude `graphify-out/` unless desired), push, `gh pr create --repo rodgi040/munkel --base platform/windows/v2-clean`.
</next_action>
