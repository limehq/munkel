---
context: default
phase: 3-notch-reply-polish
task: 0
total_tasks: 6
status: in_progress
last_updated: 2026-06-25T14:22:00+02:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through failure this session.

- [ ] CONSTRAINT: GitHub repo scope — `gh` defaults to `limehq/munkel`; always pass `--repo rodgi040/munkel` for fork PRs/commits.
- [ ] CONSTRAINT: Phase 2 is NOT on `v2-clean` — its commits live only on `swift-windows-interop` / `notch-reply-polish`. Do NOT re-branch Plan 01 from a bare `v2-clean` (drops interop vectors). Plans 02–04 wait for PR #12 merge.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong gh repo | `gh pr create` without `--repo rodgi040/munkel` targets upstream | blocking | Always pass `--repo rodgi040/munkel` |
| Re-branch from bare v2-clean | Phase 2 vectors absent on v2-clean until PR #12 merges | blocking | Stay on `notch-reply-polish`; merge PR #12 before new branches |
| Committing regenerated artifacts | `scripts/interop-vectors/vectors.json` + `graphify-out/` change on test/graph runs | advisory | Stage only intended files; exclude these unless intended |
| gsd-sdk query subcommands | `gsd-sdk query commit/init.resume` not available in installed SDK | advisory | Use plain git + read State.md/Roadmap.md |

<current_state>
Branch: `platform/windows/notch-reply-polish` (off `platform/windows/v2-clean`, Phase 2 stacked on it).

Two commits this session beyond Phase 2's WIP:
- `2e4d24d` — Phase 2 interop vectors + initial agent plans (pushed; PR #12 open)
- `413ad0a` — corrected all six agent plan files per code-reviewer subagent (local only, not pushed)

Private planning in gitignored `State.md` / `Roadmap.md`; agent plans in `apps/windows/docs/plans/`. No `.planning/phases/` structure.
</current_state>

<completed_work>

Completed this session:
- /gsd-resume-work: reconstructed `.planning/STATE.md`
- Authored sequential agent plans in `apps/windows/docs/plans/` (README + phase-2 + 01–04)
- Committed Phase 2, pushed branch, opened PR #12 → v2-clean
- Ran critical code-reviewer subagent on plans (verdict CONCERNS)
- Fixed all six plan files from review findings (commit 413ad0a)
</completed_work>

<remaining_work>

Plan 01 (notch-reply-polish) — not started:
- Task 1: Remove `.message-row` onClick; add explicit Reply button (Option A) in `NotchWidget.tsx`
- Task 2: `.reply-button` styles, sibling of `.copy-button`, `aria-label="Reply"`
- Task 3: Update `apps/windows/docs/ui-spec.md` (~lines 181–182, click-to-reply → explicit button)
- Task 4: Manual verify (`bun run dev`)
- Task 5: `bun run typecheck --filter=@munkel/windows` + `bun run test --filter=@munkel/windows`
- Task 6: PR → `platform/windows/v2-clean` with `--repo rodgi040/munkel`

Later (separate branches, after PR #12 merge): Plan 02 GitHub OAuth (optional v1), Plan 03 Windows CI, Plan 04 packaging.
</remaining_work>

<decisions_made>

- Plan 01 stays on existing `notch-reply-polish` branch (Phase 2 stacked); no re-branch from bare v2-clean.
- Plans corrected before agent execution (PR #11→#12, avatar.ts not avatar-codec.ts, added identity/IPC wiring + security/logout tasks to Plan 02).
- Default notch reply design = Option A (explicit Reply button).
</decisions_made>

<blockers>
- None technical.
- macOS `swift test --filter InteropVectorsTests` still pending (human, non-blocking).
- 413ad0a not yet pushed (user decision pending).
</blockers>

## Required Reading (in order)

1. `apps/windows/docs/plans/01-notch-reply-polish.md` — the next task to implement
2. `apps/windows/docs/plans/README.md` — branch workflow + Phase 2 / PR #12 caveat
3. `State.md` — private integration state, fork-split strategy
4. `AGENTS.md` (repo root) — branch rules, no self-merge

## Infrastructure State

- Dev server: stopped (`dev.mjs` auto-picks port 5174+; 5173 = Ultra-Wisper-Flow)
- Relay (`apps/server` :8787): not needed for notch UI work
- PR #12 open: https://github.com/rodgi040/munkel/pull/12

<context>
Munkel Windows is already usable for daily messaging. Remaining work is polish + parity + release. The notch reply field currently auto-opens on any message-row click even after a successful reply — Plan 01 fixes this with an explicit Reply button.
</context>

<next_action>
Start with: implement Plan 01 Task 1 in `apps/windows/src/renderer/components/NotchWidget.tsx` — remove `onClick={() => setReplying(true)}` from `.message-row` and add an explicit Reply button (Option A). Then follow tasks 2–6 in `apps/windows/docs/plans/01-notch-reply-polish.md`.
</next_action>
